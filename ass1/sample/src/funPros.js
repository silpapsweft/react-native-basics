import React from 'react'
import { View, Text } from 'react-native'

const funPros = (props) => {
    return (
        <View>
            <Text>
                {props.data}
            </Text>
        </View>
    )
}

export default funPros
